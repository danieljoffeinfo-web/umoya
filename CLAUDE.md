# Umoya

Static landing page. Deployed on Vercel.

## Stack
- Static HTML/CSS/JS
- Vercel deploy (`vercel.json`)

## Structure
- `index.html` — main entry
- `Umoya Landing.html` — landing page variant
- `assets/`, `uploads/` — static assets
